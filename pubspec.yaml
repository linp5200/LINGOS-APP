name: lingos_app
description: "LING OS App - Flutter 重写版（协议 v3）"
publish_to: 'none'
version: 2.0.1+5  # App 2.0.1.0_rc（versionCode=5）

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  path_provider: ^2.1.3
  go_router: ^14.2.0
  web_socket_channel: ^3.0.0
  dio: ^5.4.0
  crypto: ^3.0.3
  shared_preferences: ^2.2.3
  flutter_secure_storage: ^9.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
